import 'package:flutter_test/flutter_test.dart';
import 'package:riverpod/riverpod.dart';
import 'package:todo_mvvm/viewmodels/login_view_model.dart';

void main() {
  // test('LoginViewModel logs in successfully', () async {
  //   final authRepository = MockAuthRepository();
  //   when(() => authRepository.login(any(), any()))
  //       .thenAnswer((_) async => User(email: 'test@example.com'));

  //   final viewModel = LoginViewModel(authRepository);
  //   await viewModel.login('test@example.com', 'password123');

  //   expect(viewModel.state, isA<AsyncData>());
  // });
}
